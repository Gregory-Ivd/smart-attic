# Smart Attic — лендинг копірайтера

Особистий односторінковий лендинг **Григорія Йвд** (бренд «Smart Attic»): копірайтинг,
лендинги під ключ, SEO, AI-процес. Трьохмовний — **UA / RU / EN**.

Статичний сайт, без бекенду. Хоститься на GitHub Pages (як testsAnna).

## Структура
- `index.html` — увесь лендинг + переклади + перемикач мов (один файл).
- `.nojekyll` — щоб GitHub Pages не чіпав файли через Jekyll.

## Як редагувати тексти
Усі тексти лежать у JS-словнику `T = { ua:{…}, ru:{…}, en:{…} }` всередині
`index.html` (внизу, у `<script>`). Кожен елемент на сторінці має атрибут
`data-i18n="ключ"` — при перемиканні мови підставляється `T[мова][ключ]`.

Щоб змінити текст — правиш значення під потрібним ключем у всіх трьох мовах.
Щоб додати новий блок — додаєш елемент з `data-i18n="новий_ключ"` і сам ключ у три мови.

Мова за замовчуванням — українська; вибір користувача запам'ятовується в `localStorage`.

## Стан на 2026-05-23 (готово до публікації)
- **Голос** — дерзкий «без води». Ім'я: **Григорій Йвд** (EN: Gregory Ivd), бренд «Smart Attic».
- **3 мови під різну аудиторію** (не калька): UA — укр. бізнес; RU — російськомовні (Україна/діаспора, **не** РФ); EN — міжнародні + локалізація UA/RU→EN (м'якший тон).
- **Секції:** hero + статистика (12+ · 3 мови · 5+ галузей), послуги, кейси (живі посилання), **«Без ілюзій»** (4 правила-тейки), **«Про мене»** (фото `photo.jpg` = скетч «Лого 4») + флекс «написаного — на три томи Гюго», **«Від біржі до коду»** (01→02→03, + «цей сайт теж зроблений з AI»), «Як ми працюємо», FAQ, контакт.
- **Палітра** — тёмний ізумруд `#0a5f49`, узгоджено всюди через `--accent`. Контакт — сплошний ізумрудний блок. CTA: «Обговорити проєкт — 15 хв».
- Анімації (поява при скролі + маркер-підсвітка чисел), перемикач мов (localStorage), перо-фавікон.
- Email у контакті: `apshanuivd@gmail.com`. eText з кейсів прибрано.

## Далі (на завтра)
- [ ] **Деплой** (на паузі за рішенням власника). `gh` НЕ встановлено → піднімати через **GitHub Desktop** (Publish repository, public) або створити порожній репо `smart-attic` і `git push`. Потім Pages: Settings → Pages → `main` / root → `gregory-ivd.github.io/smart-attic/`.
- [ ] **Відгуки** — додати блок «Відгуки», коли будуть 2–3 реальні (порожнє не плодимо).
- [ ] Опц.: цифри/результати в кейсах, прайс/пакети, форма захоплення заявок (Apps Script як у testsAnna).
- Вихідники аватара (`Лого 1/2/4`, `Логи 3`, `Лого М`) — у `.gitignore`; на сайт іде лише `photo.jpg`.
